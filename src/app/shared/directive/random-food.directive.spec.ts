import { RandomFoodDirective } from './random-food.directive';

describe('RandomFoodDirective', () => {
  it('should create an instance', () => {
    const directive = new RandomFoodDirective(null);
    expect(directive).toBeTruthy();
  });
});
